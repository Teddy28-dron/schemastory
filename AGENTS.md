# 任务：从零构建一个具备获得 100+ GitHub Stars 潜力的开源项目

你将作为产品经理、开源项目维护者、软件架构师、全栈开发工程师和技术写作者，帮助我从零设计并实现一个具有真实使用价值、容易演示、容易传播、能够持续维护的开源项目。

## 一、最终目标

构建并发布一个完整的 GitHub 开源项目。

项目应具备以下特征：

- 解决一个明确、真实且高频的问题；
- 用户能够在 3 分钟内理解项目价值；
- 用户能够在 10 分钟内完成安装并看到结果；
- 项目效果可以通过截图、GIF、短视频或在线 Demo 直观展示；
- 不依赖昂贵硬件或付费 API 才能运行；
- 默认支持本地运行；
- 有清晰的目标用户，而不是面向“所有人”；
- 第一版范围足够小，能够完成，而不是设计成庞大的平台；
- 具备一定差异化，不是简单复制已有热门仓库；
- 代码结构清晰，便于其他开发者阅读和贡献；
- 项目名称简短、容易记忆、容易搜索；
- 项目具有获得 100+ GitHub Stars 的传播潜力，但不要通过刷 Star、诱导 Star 或垃圾营销实现。

不要一开始直接写代码。先完成调研、选题和规划，再进入实现阶段。

---

## 二、项目选题原则

选题不要局限于摄影测量、遥感、计算机视觉或我的专业，可以考虑：

- AI 开发工具；
- 本地大模型工具；
- 开发者效率工具；
- 命令行工具；
- 浏览器扩展；
- 数据可视化工具；
- 文件管理与知识管理；
- Git、Docker、Linux、终端辅助工具；
- 学习辅助工具；
- 隐私优先的本地应用；
- 自动化工作流；
- 小型但高价值的 Web 应用；
- 开源数据处理工具；
- 面向学生、研究者或开发者的实用工具。

优先选择符合以下条件的问题：

1. 用户现在正在使用重复、繁琐或容易出错的方法解决；
2. 现有工具过于复杂、昂贵、封闭或不支持本地运行；
3. 可以用一句话描述价值；
4. 可以通过“使用前与使用后”的对比展示效果；
5. 搜索该问题时，能找到真实用户抱怨或反复提问；
6. MVP 可以由一个人完成；
7. 不需要大规模训练数据、大量服务器资源或长期运营；
8. 可以在后续逐步增加插件、适配器、模板或社区贡献。

避免以下项目：

- 普通 Todo List；
- 普通聊天机器人；
- 没有差异化的 RAG Demo；
- 只调用一次大模型 API 的套壳项目；
- 又一个博客系统；
- 又一个天气应用；
- 功能很多但没有核心场景的“全能平台”；
- 依赖无法公开的数据或内部环境；
- 需要先建立大量用户网络才有价值的产品；
- 只能通过文字描述、无法直观演示价值的项目。

---

## 三、第一阶段：调研和候选项目生成

先检查当前仓库中的已有文件、代码、开发环境和约束。

随后生成 10 个候选项目。每个候选项目都需要包含：

- 暂定项目名；
- 一句话介绍；
- 目标用户；
- 用户正在遭遇的具体问题；
- 典型使用场景；
- 当前替代方案；
- 现有方案的不足；
- 我们的差异化；
- 最核心的一个功能；
- 可以展示的 Demo；
- MVP 预计包含哪些功能；
- 明确不做哪些功能；
- 推荐技术栈；
- 实现难度；
- 维护成本；
- 潜在传播渠道；
- 获得 Star 的主要理由；
- 项目失败的主要风险。

不要为了显得创新而编造需求。候选项目必须解决真实存在的问题。

对候选项目按照以下维度进行 1～10 分评价：

- 问题真实程度；
- 用户痛点强度；
- 目标用户规模；
- 差异化；
- 演示效果；
- 开发可行性；
- 首次使用门槛；
- 自传播能力；
- 后续扩展空间；
- 长期维护成本。

创建一个评分表，并说明评分依据。

最后选出最值得实现的 3 个项目，分别给出支持和反对它们的理由。

---

## 四、第二阶段：验证选题

对于排名最高的 3 个项目，进一步进行竞品和需求验证。

重点验证：

- 是否已经存在高度相似的热门开源项目；
- 相似项目最近是否仍在维护；
- 相似项目获得用户欢迎的原因；
- 相似项目的 Issues 中有哪些高频抱怨；
- 用户通常使用什么关键词描述这个问题；
- 我们是否能提供足够明确的改进；
- 用户为什么会选择我们的项目，而不是现有项目；
- 第一版是否能在较小范围内实现完整体验。

输出竞品对比表，至少包含：

- 项目名称；
- 核心定位；
- 主要功能；
- 优点；
- 缺点；
- 活跃程度；
- 使用门槛；
- 我们可以学习的地方；
- 我们可以避开的地方；
- 我们的差异化机会。

如果没有足够证据表明某个项目解决了真实需求，应主动淘汰该项目。

最终确定一个项目，并用下面的格式说明：

> 为【目标用户】提供一个【产品类别】，帮助他们在【场景】下解决【具体问题】。与【现有方案】相比，本项目通过【核心差异化】让用户能够【可量化或可感知的收益】。

---

## 五、第三阶段：定义 MVP

在开始编码之前，编写一份简洁的产品需求文档。

必须明确：

### 1. 核心用户流程

描述用户从发现项目到获得结果的完整路径：

1. 用户为什么来到仓库；
2. 用户看到 README 后理解了什么；
3. 用户如何安装；
4. 用户执行什么命令或操作；
5. 用户得到什么结果；
6. 用户为什么愿意继续使用、分享或 Star。

### 2. MVP 功能

只保留构成完整价值闭环的功能。

将功能划分为：

- P0：没有它项目就没有价值；
- P1：能显著改善体验，但可以后做；
- P2：未来增强项；
- 明确不做的功能。

### 3. 成功标准

至少定义以下指标：

- 新用户首次成功运行所需时间；
- 安装步骤数量；
- 核心任务完成时间；
- Demo 是否可以直观体现价值；
- 是否可以在没有付费服务的情况下运行；
- 是否具有跨平台能力；
- 是否容易编写教程和传播内容。

### 4. 技术方案

技术选择遵循以下原则：

- 优先成熟、稳定、文档完善的依赖；
- 尽量减少依赖数量；
- 避免不必要的微服务；
- 避免过度设计；
- 默认提供简单的本地启动方式；
- 必要时提供 Docker；
- 关键模块需要可测试；
- 配置、数据和业务逻辑分离；
- 错误信息必须可理解；
- 不得把密钥、Token 或个人路径写入仓库。

---

## 六、第四阶段：项目实现

按照小步迭代方式实现，不要一次生成大量未经验证的代码。

推荐顺序：

1. 建立最小目录结构；
2. 实现核心功能最短闭环；
3. 编写最小测试；
4. 实际运行并修复错误；
5. 添加示例数据；
6. 改善命令行或界面体验；
7. 补充异常处理；
8. 补充安装方式；
9. 完善文档；
10. 制作 Demo。

每完成一个阶段，都应：

- 运行项目；
- 执行测试；
- 检查静态分析或格式化；
- 说明当前已经完成的功能；
- 记录仍存在的问题；
- 更新任务清单。

禁止：

- 只创建代码骨架而不实现；
- 使用大量 TODO 代替代码；
- 编造测试通过结果；
- 在没有运行的情况下声称项目可用；
- 为了显得专业引入复杂架构；
- 在核心功能尚未完成前开发后台管理、用户系统和支付系统；
- 生成无法验证的性能数据。

---

## 七、工程质量要求

项目至少应包含：

- 清晰的源代码目录；
- 单元测试或集成测试；
- 示例配置；
- 示例输入和输出；
- `.gitignore`；
- 开源许可证；
- 贡献指南；
- Issue 模板；
- Pull Request 模板；
- Changelog；
- Roadmap；
- 安全说明；
- Code of Conduct；
- 自动化测试工作流；
- 格式化与代码检查配置；
- 合理的版本号；
- 可复现的安装步骤。

代码要求：

- 函数和模块职责明确；
- 避免超大文件；
- 关键逻辑写清楚注释；
- 对外接口保持简单；
- 错误信息告诉用户“发生了什么”和“如何处理”；
- 对可选依赖进行优雅降级；
- 对输入进行必要验证；
- 测试覆盖核心路径和主要异常路径；
- 所有文档中的命令都应实际验证。

---

## 八、README 要求

README 是项目获得用户和 Star 的核心，不要把它写成代码说明书。

README 顶部应依次包含：

1. 项目名称；
2. 一句话价值主张；
3. 一张最能体现价值的 GIF、截图或结果图；
4. 3～5 个最重要的特点；
5. 30 秒快速体验；
6. 适用场景；
7. 安装方式；
8. 基础示例；
9. 使用前后对比；
10. 与相似项目的区别；
11. 工作原理；
12. Roadmap；
13. 贡献方式；
14. License。

README 必须同时回答：

- 这是什么？
- 它解决什么问题？
- 为什么不直接使用现有工具？
- 谁适合使用？
- 我怎样最快看到效果？
- 它是否安全、免费、本地运行？
- 项目目前完成到什么程度？
- 我怎样参与贡献？

README 开头不要堆叠十几个徽章，也不要先讲复杂架构。

生成英文 README 作为主版本，并根据项目目标用户决定是否增加中文 README。

---

## 九、Demo 和视觉展示

为项目设计至少一种高传播性的演示形式：

- 15～30 秒 GIF；
- 使用前后对比图；
- 终端录屏；
- 交互式网页 Demo；
- 示例结果画廊；
- 一条命令运行的在线环境；
- 可复制的最小示例。

Demo 应体现：

- 用户原来的问题；
- 用户执行的操作；
- 项目生成的结果；
- 项目比原方案节省了什么。

不要只展示安装过程。

为 Demo 准备：

- 演示脚本；
- 示例数据；
- 推荐录制尺寸；
- 每一步需要显示的内容；
- README 中的配套说明；
- 用于社交媒体发布的简短描述。

---

## 十、开源发布和传播方案

在完成 MVP 后，生成一份合规、非垃圾营销式的发布方案。

至少包括：

- GitHub 仓库描述；
- GitHub Topics；
- 项目 Logo 或视觉识别建议；
- 第一个 Release 的标题和说明；
- Hacker News 发布文案；
- Reddit 发布文案；
- X/Twitter 发布文案；
- LinkedIn 发布文案；
- 中文技术社区发布文案；
- 适合发布的相关社区；
- 一篇技术博客的大纲；
- 一段 30 秒产品介绍；
- 一份 7 天发布节奏；
- 一份 30 天迭代计划。

传播内容必须具体说明项目价值，不能只写：

- “我做了一个很酷的项目”；
- “欢迎大家 Star”；
- “这是一个 AI 驱动的平台”。

更好的表达应包括：

- 用户原来需要做什么；
- 使用项目后只需要做什么；
- 可观察到的结果；
- 项目适合哪些人；
- 项目与现有工具的区别。

不得进行刷 Star、互 Star、购买 Star、批量私信或无关社区推广。

---

## 十一、发布后的反馈闭环

为项目设计反馈和迭代机制：

- 在 README 中提供反馈入口；
- 创建功能请求模板；
- 创建 Bug 模板；
- 收集安装失败原因；
- 记录用户首次运行耗时；
- 识别用户在哪一步退出；
- 汇总重复出现的 Issue；
- 优先修复阻碍首次体验的问题；
- 根据真实反馈调整 Roadmap。

发布后优先级应为：

1. 修复安装失败；
2. 修复核心功能错误；
3. 改善首次使用体验；
4. 增加真实用户请求的功能；
5. 最后再扩展架构。

---

## 十二、执行方式

请在仓库中维护以下文件：

- `PROJECT_IDEAS.md`：候选项目和评分；
- `COMPETITOR_ANALYSIS.md`：竞品分析；
- `PRD.md`：产品需求文档；
- `ARCHITECTURE.md`：技术方案；
- `ROADMAP.md`：开发计划；
- `TASKS.md`：任务状态；
- `CHANGELOG.md`：版本变化；
- `LAUNCH_PLAN.md`：发布计划；
- `README.md`：项目主页。

工作过程中遵循：

1. 先阅读已有文件；
2. 先调研，再做决策；
3. 先完成核心闭环，再扩展功能；
4. 每次只处理一个清晰任务；
5. 修改后立即运行和验证；
6. 不删除已有有效内容；
7. 不掩盖错误；
8. 对不确定的信息明确标记；
9. 所有重要决策记录在文档中；
10. 最终结果必须能够由新用户按照 README 独立运行。

---

## 十三、当前第一项任务

现在不要直接开发最终项目。

先完成以下任务：

1. 分析当前仓库和开发环境；
2. 提出 10 个不局限于任何专业的开源项目候选；
3. 对候选项目进行评分；
4. 选择最有潜力的 3 个；
5. 给出初步竞品分析；
6. 推荐一个最值得开发的方向；
7. 将结果写入 `PROJECT_IDEAS.md`；
8. 暂停编码，等待项目方向确定后再继续。

在结论中必须明确说明：

- 为什么这个问题值得解决；
- 为什么用户可能愿意传播这个项目；
- 为什么现有项目没有完全解决它；
- MVP 最吸引人的 Demo 是什么；
- 实现过程中最大的技术和产品风险是什么。
